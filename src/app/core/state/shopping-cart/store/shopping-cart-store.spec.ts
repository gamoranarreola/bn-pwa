import { ShoppingCartStore } from './shopping-cart-store';

describe('ShoppingCartStore', () => {
  it('should create an instance', () => {
    expect(new ShoppingCartStore()).toBeTruthy();
  });
});
