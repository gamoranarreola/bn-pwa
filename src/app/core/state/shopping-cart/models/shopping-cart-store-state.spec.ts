import { ShoppingCartStoreState } from './shopping-cart-store-state';

describe('ShoppingCartStoreState', () => {
  it('should create an instance', () => {
    expect(new ShoppingCartStoreState()).toBeTruthy();
  });
});
